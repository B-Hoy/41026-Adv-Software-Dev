{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make jar",
            "type": "shell",
            "command": "jar -cvf ASD.war src/main/webapp/*",
			"windows": {
				"command": "jar -cvf ASD.war src\\main\\webapp\\*"
			},
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
